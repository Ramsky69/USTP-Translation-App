# Set a real API key or bearer token in a local `.env` file.
# For example:
# GEMINI_API_KEY=YOUR_API_KEY_OR_BEARER_TOKEN
# USE_API_KEY=true   # (optional - set to true if you want the server to pass the key as ?key=...)
# Optionally set API_BASE_URL if you use a proxy or a custom endpoint
# API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
